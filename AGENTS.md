# DeviceCare Agent 项目规则

## 开始工作前

1. 确认实际仓库根目录为 `D:\DeviceCare_Agent\DeviceCareAgent`。
2. 阅读本文件和 `HANDOFF.md`。
3. 按任务读取对应的唯一权威文档，不要先遍历全部资料。
4. 运行 `git status --short --branch`，保留用户已有改动。

## 文档职责

| 内容 | 唯一权威 |
|---|---|
| 当前状态、阻塞项、下一步 | `HANDOFF.md` |
| 产品目标、MVP、验收口径 | `PROJECT_PROPOSAL.md` |
| 技术选型、模块边界、数据与部署 | `docs/architecture.md` |
| 多 Agent 任务契约、分工与质量门 | `docs/development-workflow.md` |
| 可复用的失败教训 | `AGENT_LESSONS.md` |
| 项目入口和阅读导航 | `README.md` |

- 一个结论只在一处完整描述，其他文件只放一句摘要和链接。
- 新建文档前，先确认现有权威文档是否能容纳该内容。
- ADR 只在出现需要长期保留的重大架构变更时创建；当前方案直接维护在 `docs/architecture.md`。
- 冲突时：执行规则以 `AGENTS.md` 为准；实时状态以 `HANDOFF.md` 为准；产品和技术分别回到各自权威文档。

## 开发原则

- 先写清假设和验收标准，再改代码。
- 优先最小可用实现；不为未来场景预建抽象、服务或配置。
- 修改要局部、可回滚，并直接解决当前问题。
- 复杂度增长前先证明需求：第二个真实实现出现后再抽象，达到拆分触发条件后再拆服务。
- 验证与风险相称：至少运行受影响范围的检查，并记录没有覆盖的验证。

## 多 Agent 协作

- 先定流程，再配 Agent。新流程先由主 Agent 完整跑通并记录实际摩擦；只有输入、步骤和输出足够稳定且会重复使用时，才提炼为 Skill。
- 小而明确的任务由主 Agent 直接完成。只有任务可以清晰分界，且委派能明显提升速度、专业性或独立验证质量时，才调用子 Agent。
- 默认使用 0–3 个按需子 Agent，不维持固定团队。优先并行只读调研、代码探索、测试和审查；并行写入必须拥有互不重叠的文件范围。
- 主 Agent 负责拆解任务、冻结共享契约、分配文件、集成结果、运行最终验证和更新交接。子 Agent 不互相调度，也不得自行扩大范围或继续拆分任务，除非主 Agent 明确授权。
- 委派前必须写明目标、输入、文件所有权、禁止修改范围、交付物、验收标准、验证方法和停止条件。具体格式见 `docs/development-workflow.md`。
- `AGENTS.md`、`HANDOFF.md`、产品与架构权威文档、共享 Schema、锁文件和公共配置默认由主 Agent 持有；需要子 Agent 修改时必须逐项明确授权。
- 实现者的完成声明不能代替验收。主 Agent 或独立审查 Agent 必须检查差异和验证证据；测试失败、契约冲突或信息不足时，不得自动把任务传给下一角色。
- 子 Agent 遇到范围扩张、破坏性操作、发布部署、提交推送或需要产品决策的事项时，必须停止并回报主 Agent，由主 Agent 向用户确认。

## 产品与安全边界

- `customer_answer` 面向用户，`agent_note` 仅供客服内部使用，禁止混用。
- 库存、保修、维修进度等事实只能来自工具或可信系统；查询失败时不得猜测。
- 低置信度、高风险或需人工承诺的场景必须转人工。
- 演示数据、目标值和真实业务结果必须明确区分。
- 官网 Widget 是客服诊断闭环验证后的阶段二，不得为了“一行嵌入”提前建设通用平台、多租户或多模型管理后台。
- 模型密钥只能保存在服务端；公开 Widget 不得返回 `agent_note`、内部诊断依据或敏感工具原始结果。

## 文件与 Git 安全

- 禁止递归或批量删除。删除时一次只处理一个经过确认的明确路径。
- 禁止 `git clean`、`git reset --hard` 和覆盖用户改动。
- 未经用户明确授权，不提交、不推送、不创建 PR。
- 只暂存本任务涉及的文件。
