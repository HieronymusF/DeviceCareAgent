# Agent lessons

这里只记录尚未固化到其他权威文档的可复用教训；已经固化的规则只保留指针，不再复制正文。

## 已固化

- 外层工作区与 Git 根目录的区别 → `AGENTS.md`“开始工作前”。

## 外部评审建议先过 MVP 证据门

- 失败模式：把评审意见逐条转成产品承诺，导致在首品类、数据和基线尚未确定时写死任意阈值、流程或界面。
- 预防规则：逐条判断建议是否影响首个纵向切片的安全、数据契约或可验证性；立即补前两类，参数化或延后缺少真实依据的实现细节，拒绝与 MVP 目标无关的范围扩张。
- 验证方法：文档修改后列出“纳入、简化、延后”三类，检查新增内容是否有数据来源、明确责任和可执行验收。
- 证据：2026-07-25 对 Claude 两轮方案评审的批判性吸收。
- 最近确认：2026-07-25 +08:00

## 区分证据物、解决机制和业务结果

- 失败模式：看到 SOP、教程或 Demo 原件后，把最容易展示的文档直接写成“解决方案”，忽略真正产生结果的是背后的完整运营与交付体系。
- 预防规则：每个案例分别记录“业务问题 → 解决机制 → 交付物 → 直接产出 → 业务结果”；文档和工具默认属于交付物，只有在有因果证据时才允许直接归因业务结果。
- 验证方法：检查结果句的主语；若写成“某文档降低了投诉”，必须追问中间采用、执行、放量和持续运营环节，否则改由完整体系承担归因，并把结果按定量/定性证据分级。
- 代理指标规则：内部业务分母缺失时，可以用固定公开样本构造回溯代理指标，但必须同时保存采样规则、命中定义和限制；优先报告前后原始分子/分母与百分点变化，不把相对变化外推成全平台业务结果。
- 责任归属规则：历史事件能作为 Agent 评测数据源，不等于所有成果都归给用户；分别记录“知情、参与、主导、执行、审核”，并继续拆分“根因定位、方案修复、客服转译、知识标准化、用户闭环”。没有参与技术修复，不代表没有承担服务产品化贡献。
- 受控重构规则：项目可以组合真实案例、生成审核过的评测变体，并设计历史上不存在的 Agent、模拟工具和指标；但必须把“历史事实/个人职责”和“当前独立重构/实测结果”分层，不用艺术加工改变雇主、时间、生产部署或业务结果。
- 证据：2026-07-27 八爪鱼 3 案例中，用户纠正“Demo 文档解决客诉”为“适配生产体系扩大供给后降低跨平台客诉并改善舆论”；八爪鱼 4 断轴事件的硬件调查由硬件产品、结构及工厂团队完成，用户负责把结论和产品建议转成客服标准话术。
- 最近确认：2026-07-27 +08:00

## 重复案例字段必须按 case_id 校验映射

- 失败模式：在多个案例都包含相同字段名时，只用 `evidence_status` 等重复行作为补丁锚点，可能把某条业务复核备注贴到相邻案例；只统计备注总数仍会漏过这种错位。
- 预防规则：修改案例卡时，补丁上下文必须同时包含案例标题或 `case_id`；批量修改后按“标题到下一标题”的案例块逐条核对目标 case ID、复核状态和备注语义。
- 验证方法：除检查总数外，输出 `case_id → review status → note` 映射；Markdown 事件档案也必须解析每个案例块，确认备注与机器评测集对应，不能只用全文命中数代替映射校验。
- 证据：2026-07-27 复核时发现 `dc-e01-004` 的“停止重复升级”备注曾错误落在 `dc-e01-002`，评测 JSON 正确但事件档案错位。
- 最近确认：2026-07-27 +08:00

## 外部审核资源必须先确认并设计降级门

- 失败模式：在未确认可获得具备售后或硬件经验的第二评审者前，把该角色写成冻结硬门，导致案例与安全审核均完成后仍被不可控外部资源阻塞。
- 根因分类：`information-gap`。方案知道需要事实制衡，但没有先验证评审资源，也没有区分“历史事实第三方认证”和“当前受控重构评测可冻结”两个目标。
- 预防规则：凡依赖外部人员、真实系统或原始资料的验收门，立项时同时记录首选路径、可获得性和不降低安全边界的降级路径；降级路径必须写明哪些结论仍不能宣称。
- 验证方法：冻结记录同时包含审核模式、独立外部评审布尔值、历史事实认证布尔值和适用范围；若外部评审为否，所有仅由案例所有者确认的内部事实都保持来源标签，不得转成对客确定性结论。
- 证据：2026-07-27 用户确认无法找到第二名适格事实评审者，项目改用“案例所有者确认 + 证据边界审计 + 契约安全审计”的受控重构降级门。
- 最近确认：2026-07-27 +08:00
