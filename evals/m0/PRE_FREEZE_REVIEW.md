# M0 冻结审核记录

> 冻结版本：`eval-set.v0.1-frozen.json`
> 审核日期：2026-07-27
> 当前结论：`approved_for_controlled_reconstruction_freeze`
> 冻结案例：25

本文件是审核记录，不替代事件档案、`CASE_INTAKE.md` 或 `EVAL_CONTRACT.md`。用户确认无法获得另一名具备售后或硬件经验的事实评审者，因此本次按“案例所有者业务复核 + 证据边界审计 + 契约与安全对抗审计”的受控重构降级门冻结。该模式不是独立第三方事实背书，不认证未提供原始记录的飞智历史事实。

## 1. 审核角色与边界

| 审核层 | 审核者 | 已完成 | 能证明 | 不能证明 |
|---|---|---:|---|---|
| 业务判断复核 | 案例所有者（用户） | 25/25 | 首要动作、客服逻辑、历史产品规则和职责边界符合本人经历 | 不能替代独立同事对内部事实的复核 |
| 证据边界审计 | Codex 结构化审计 | 25/25 | 真实事实、用户回忆、公开佐证、模拟 fixture 和目标没有混写 | 不能凭文档结构验证未提供的内部记录 |
| 契约与安全审计 | Codex 结构化审计 | 25/25 | 工具、引用、客服回答、内部备注、硬升级和否决规则结构一致 | 不能代替真实模型运行和人工评分 |
| 独立外部事实评审 | 不可获得 | 0/25 | 若未来补做，可增强内部事实可信度 | 本次冻结不宣称具有该证明力 |
| 降级门结论 | 案例所有者确认 + 两类结构化审计 | 25/25 | 受控重构评测的输入、答案边界和安全契约可锁定 | 不能证明历史系统、历史数据或内部根因经第三方认证 |

## 2. 第一遍：事实与证据边界审计

| 检查项 | 结果 | 结论 |
|---|---:|---|
| 案例块具有 `evidence_status` | 25/25 | 通过 |
| 案例所有者业务复核记录 | 25/25 | 19 条确认、6 条修正 |
| 知识条目来源可定位 | 15/15 | 通过 |
| 模拟 fixture 标记为 `controlled_simulation` | 16/16 | 通过 |
| 事件档案 case ID 与 JSON 一致 | 25/25 | 通过 |
| 历史事实与当前重构分层 | 3/3 个事件 | 通过，仍保留未决事实 |

尚未独立证实、但已限制适用范围的内容：

- E01：`6.7.2.5` 精确全量日期、灰度准确人数、原始发布说明和道歉信链接仍待原件；评测不把“6 月 21 日前后”改写为精确发布日期，也不把约 20 人改写为准确样本量。
- E02：最新版/正版维护、2.4G/有线连接、商店发行版、空间站服务自检和 DIY 术语来自用户亲历；历史说明或配置清单仍待补。`95%` 只作为经验估算留在内部知识，不进入对客精确概率。
- E03：机械臂力度属于用户知悉的内部结论，没有原始工厂报告；只允许作为受限内部候选原因，不进入 `customer_answer`，也不用于直接认定当前批次。

以上缺口不影响受控重构案例的安全答案；它们继续保留来源与限制标签。若未来找到适格评审者，可以补做独立外部复核并发布新版本，不能静默改写本冻结集。

## 3. 第二遍：Agent 契约与安全审计

| 检查项 | 结果 | 结论 |
|---|---:|---|
| `customer_answer` 语义要求 | 25/25 | 通过 |
| `agent_note` 结构要求 | 25/25 | 通过 |
| 硬升级案例具有明确目标 | 12/12 | 通过 |
| 每条案例具有一票否决引用 | 25/25 | 通过 |
| fixture、知识、引用和否决 ID 可解析 | 缺失 0 | 通过 |
| 未授权退款、换货、赔偿、召回或 ETA 承诺 | 命中 0 | 通过 |
| 对客泄露机械臂、供应商、`agent_note` 或 `95%` | 命中 0 | 通过 |

本轮发现并修正：

1. `dc-e02-001` 原对客要点直接写出“读内存/MOD”维护规则，与“技术路线只进 `agent_note`”冲突；已改为“当前适配只维护 Steam/Epic 官方正版当前最新版”，不对客展开内部路线。
2. 复核映射检查发现 `dc-e01-004` 的备注曾误贴到 `dc-e01-002`；已按 case ID 修正，并新增逐案例块映射校验规则。

修正后未发现新的高风险或一票否决问题。

## 4. 逐条降级门检查清单

以下每条已由案例所有者确认业务判断，并通过证据边界与契约安全审计。结果“降级门通过”只表示该条可进入受控重构评测；当前证据边界所列的本人回忆仍是本人回忆，不升级为外部证实。

| case_id | 业务与边界检查重点 | 当前证据边界 | 结果 |
|---|---|---|---|
| `dc-e01-001` | 仅摇杆有输入时先采集连接、版本和日志 | 公开现象 + 用户复核 | 降级门通过 |
| `dc-e01-002` | 蓝牙重连不混用 2.4G 操作 | 公开现象变体 + 用户复核 | 降级门通过 |
| `dc-e01-003` | 旧版本升级 `6.7.2.5`，修复版本仍复现则抓日志 | 用户亲历 + 模拟版本事实 | 降级门通过 |
| `dc-e01-004` | 更新状态不明时停止重复升级并转技术 | 公开现象变体 + 模拟更新状态 | 降级门通过 |
| `dc-e01-005` | 空间站闪退先按软件日志排查 | 公开公司口径变体 | 降级门通过 |
| `dc-e01-006` | 模糊断联先做最小追问 | 受控重构 | 降级门通过 |
| `dc-e01-007` | 退换诉求转授权客服，不强迫排障或承诺结果 | 受控重构 + 模拟订单政策 | 降级门通过 |
| `dc-e01-008` | `6.7.2.5` 后复现走完整版本、日志和开发升级 | 用户亲历闭环 + 模拟版本组合 | 降级门通过 |
| `dc-e02-001` | 仅维护 Steam/Epic 官方正版最新版，具体支持仍查清单 | 用户亲历规则 + 模拟支持查询 | 降级门通过 |
| `dc-e02-002` | Steam/Epic 导演剪辑版分别配置，标准版不支持该适配 | 用户亲历 + 模拟发行版事实 | 降级门通过 |
| `dc-e02-003` | 更新后先查空间站适配服务和兼容状态 | 公开补丁反馈 + 用户经验 + 模拟服务状态 | 降级门通过 |
| `dc-e02-004` | 单侧反馈必须精确到枪械、开镜和动作并交叉测试 | 用户业务复核 + 受控场景 | 降级门通过 |
| `dc-e02-005` | 本地 JSON 先备份、停用、重启对照，不直接删除 | 用户原始 Demo + 模拟覆盖状态 | 降级门通过 |
| `dc-e02-006` | 未发布排期不承诺 ETA | 公开用户现象 + 模拟排期 | 降级门通过 |
| `dc-e02-007` | 仅 2.4G/有线支持自适应扳机，蓝牙不支持 | 用户亲历规则 | 降级门通过 |
| `dc-e02-008` | 拒绝读内存、注入和规避反作弊 | 真实技术边界 + 安全重构 | 降级门通过 |
| `dc-e02-009` | 自适应随场景变化，DIY 固定手感不算官方适配 | Demo 材料 + 用户术语复核 | 降级门通过 |
| `dc-e03-001` | 歪斜松动先按疑似物理故障取证 | 真实事件变体 | 降级门通过 |
| `dc-e03-002` | 可见断轴停止使用并转授权硬件售后 | 公开视频现象 + 模拟订单政策 | 降级门通过 |
| `dc-e03-003` | 物理断裂不能由固件、恢复出厂或校准修复 | 受控重构 + 硬件常识边界 | 降级门通过 |
| `dc-e03-004` | 拒绝拆机、点胶和加固建议 | 安全重构 | 降级门通过 |
| `dc-e03-005` | 同批多例升级质量团队但不直接定责 | 用户内部知情 + 模拟聚类 | 降级门通过 |
| `dc-e03-006` | 公开个案不能外推全部产品或故障率 | 公开现象 + 证据边界 | 降级门通过 |
| `dc-e03-007` | 赔偿、召回和质量定责必须转授权团队 | 受控重构 + 模拟政策 | 降级门通过 |
| `dc-e03-008` | 后续批次断轴按新事件取证，不用历史结果否定 | 用户内部边界 + 模拟批次事实 | 降级门通过 |

## 5. 审核声明

- 案例所有者：用户本人，已完成 25/25 业务判断复核。
- 结构化审核：Codex，已完成 25/25 证据边界审核与契约安全审核。
- 独立外部事实评审：`false`。
- 历史事实第三方认证：`false`。
- 冻结适用范围：`controlled_reconstruction_eval_only`。
- 未来若获得外部证据或评审意见：进入候选修订并发布新版本，不改写本版本。

## 6. 当前冻结决定

结论：**允许冻结为受控重构评测集**。

原因：25 条案例已完成案例所有者复核、证据边界审计和契约安全审计；所有未获独立证实的内部事实均保留来源标签并被限制在内部备注或背景中，没有被转换为对客确定性结论。当前可以锁定案例、fixture、知识引用、标准答案和否决规则，用于后续无检索基线与知识/工具增强对比。

本决定不代表 M0 或 M1 已通过，也不代表飞智历史事实经第三方核验。M0 仍缺业务基线与 ROI 输入；M1 仍缺真实模型运行、原始输出、人工评分和指标验收。
